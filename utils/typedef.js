/**
 * @typedef {Object} user
 * @property {string} user.username
 * @property {string} user.email
*/

/**
 * @typedef {Object} thought
 * @property {string} thought.thoughtText
 * @property {string} thought.username
 * @property {Array<reaction>} thought.reactions
 */

/**
 * @typedef {Object} reaction
 * @property {string} reaction.reactionBody
 * @property {string} reaction.username
 */

module.exports.unused = {};